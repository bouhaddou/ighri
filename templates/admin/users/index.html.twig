{% extends 'admin/base.html.twig' %}

{% block title %}Administration des annonces
{% endblock %}

{% block body %}

	<div class="container-fluid">
		<h2 class="my-1 mt-4">Gestion des Utilisateurs</h2>
		<table class="table table-hover mt-4">
			<thead>
				<tr>
					<th>Id</th>
					<th>Nom & Prénom</th>
					<th>Image</th>
					<th>Email</th>
					<th>Prfile</th>
					<th>Description</th>
					<th>Actions</th>
				</tr>
			</thead>
			<tbody>
				{% set users = pagination.getData %}
				{% for user in users %}
					<tr>
						<td>{{ user.id }}</td>
						<td>{{ user.getFullname | slice(0,40) }}
							</td>
						<td>
							<img alt="avatar " src="{{asset('/upload/users/' ~ user.avatar ) }}" class="avatar avatar-mini">
							{{ user.getFullname }}</td>
						<td>
							{{ user.email }}
						</td>
                        <td>
							{{ user.slug }}
						</td>
                         <td>
							{{ user.content | slice(0,60) | raw}} ......!!
						</td>
						<td>
							<a href="" class="bn  p-1">
								<i class="fas fa-trash text-danger"></i>
							</a>
						</td>
					</tr>
				{% endfor %}

			</tbody>
		</table>
		<div class="d-flex justify-content-center">
			{{ pagination.display() }}
		</div>
	</div>
{% endblock %}

