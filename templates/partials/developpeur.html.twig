 <div class="sidebar-box ftco-animate">
	<h3 class="heading text-center">Menu</h3>
	<hr>
	<ul class="categories">
		<li><a href=" {{ path('presentationpage') }} " class="text-primary">Ighri <span> </span></a></li>
		<li><a href="{{  path('postpage') }}"class="text-primary">Publication  <span>({{ posts| length }})</span></a></li>
		<li><a  href="{{ path('imagepage') }}"class="text-primary">Images <span>(37)</span></a></li>
		<li><a href="{{ path('vediopage') }}"class="text-primary">Vedio <span>(42)</span></a></li>
	</ul>
</div>
<div class="sidebar-box ftco-animate">
	<h3 class="heading">publications récente</h3>
	{% for TopPost in TopPosts %}
	<div class="block-21 mb-4 d-flex">
		<a class="blog-img mr-4" style="background-image: url({{ asset("./upload/posts/"~ TopPost.avatar) }}"></a>
		<div class="text">
			<h3 class="heading-1"><a href="#">{{ TopPost.content| slice(0,70)  }}</a></h3>
			<div class="meta">
				<span class="clearfix" style="font-size: 13px; line-height: 0em;">
						{{ TopPost.datepub | date('d-m-Y') }} <i class="far fa-clock"></i>
						<a href="#" class="text-danger">
						{% set user = 'Utilisateur'%}
						{%  for role in TopPost.author.roles  %}
							{% if role == 'ROLE_ADMIN' %}
								{% set user = 'Admin'%}
								{% else %}
							{% endif %}
						{% endfor %}
						{{ user }}
					</a><i class="fas fa-users"></i>
				</span>
				<hr>
			</div>
		</div>
	</div>
	{% endfor  %}
</div>
            <div class="sidebar-box ftco-animate">
              <h3 class="heading text-center text-secondary">Les Membres de Ighri</h3>
              <div class="tagcloud text-center">
			{% for user in users %}
			   {% if app.user %} 
				{% if user.id == app.user.id  %}
				{%  else %}
				<img width="60%" height="80px" class="img-fluid text-center mb-1" src="{{ asset("./upload/users/"~ user.avatar) }}"><br>
				<a href="{{ path('showprofile',{'id': user.id}) }}" class="tag-cloud-link"><h5 class="text-primary">{{user.getFullname}}</h5></a><br>
				{% endif %}
			{% else %}
				<img width="60%" height="80px" class="img-fluid text-center mb-1" src="{{ asset("./upload/users/"~ user.avatar) }}"><br>
				<a href="{{ path('showprofile',{'id': user.id}) }}" class="tag-cloud-link"><h5 class="text-primary">{{user.getFullname}}</h5></a><br>
				
			{% endif %}
				{% endfor %}
              </div>
            </div>

       