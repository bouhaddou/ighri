<section>

<div class="container mt-4 mb-2">
    <h3 class="text-center">Les Vidéos de  | <span class="text-primary"> IGHRI </span></h3>
        <div id="carouselExampleCaptions" class="carousel slide" data-ride="carousel" data-interval="false">
        <ol class="carousel-indicators">
            
            <li data-target="#carouselExampleCaptions" data-slide-to="0" class="active"></li>
            {% for vedio in vedios %}	
            <li data-target="#carouselExampleCaptions"data-slide-to="{{loop.index0 + 1}}"></li>
            {% endfor %}
        </ol>
        <div class="carousel-inner">
            <div class="carousel-item active">
                <iframe width="100%" height="315" src="https://www.youtube.com/embed/pnk12YethiY" frameborder="0" 
                allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
            </div>


        {% for vedio in vedios %}
            {% if vedio.valider == true %}
                <div class="carousel-item  ">
                <div class="d-block w-100" style="max-width:100%; max-height: 315px;">
                    {% if vedio.type == "Youtube" %}
                                <iframe  width="560" height="315" src="//www.youtube.com/embed/{{vedio.lien|trim|slice(32,300)|trim }}" 
                                    frameborder="0" allowfullscreen>
                                </iframe> 
                    {% elseif vedio.type == "Facebook"   %}
                                {{ vedio.lien | raw}}

                                    {% if '</iframe>' in vedio.lien %}
                                    {% else %}
                                    </iframe>
                                    {% endif %}
                    {% else %}

                    {% endif %}
                </div>
                <div class="carousel-caption d-none d-md-block">
                    <h5>{{ vedio.titre }}</h5>
                    <p>{{ vedio.content }}</p>
                </div>
                </div>
            {% endif %}
        {% endfor %}
        </div>
        <a class="carousel-control-prev mt-5" href="#carouselExampleCaptions" role="button" data-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="sr-only">Previous</span>
        </a>
        <a class="carousel-control-next mt-5" href="#carouselExampleCaptions" role="button" data-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="sr-only">Next</span>
        </a>
        </div>
        <div class="text-center mt-3 mb-2">
				<a class="dropdown-item h2" href="{{ path('vediopage') }}" ><i class="fas fa-video   "></i> voir plus </a>
			</div>
    </div>
</section>
